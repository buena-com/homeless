{
  "name": "homeless",
  "description": "A collection of reuasble project agnostic typescript packages.",
  "repository": {
    "type": "git",
    "url": "git+https://git@github.com:home-ht/homeless.git"
  },
  "private": true,
  "scripts": {
    "lerna-publish": "lerna publish",
    "lerna-boostrap": "lerna bootstrap",
    "test": "jest --no-cache --coverage",
    "build": "lerna run build"
  },
  "devDependencies": {
    "@types/jest": "^22.2.3",
    "jest": "^22.4.3",
    "lerna": "^3.6.0",
    "ts-jest": "^22.4.5",
    "typescript": "^2.8.3"
  }
}
