{
  "name": "@home-ht/serverless",
  "version": "1.3.0",
  "description": "A collection helper utils for serverless lambda function etc",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "publishConfig": {
    "access": "restricted"
  },
  "scripts": {
    "test": "jest --no-cache --watch --config=../../jest.config.js",
    "prepare": "npm run build",
    "build": "rm -rf lib && tsc -p tsconfig.build.json"
  },
  "license": "MIT",
  "dependencies": {
    "getenv": "^0.7.0"
  },
  "devDependencies": {
    "@home-ht/common": "^1.3.0",
    "@types/aws-lambda": "^8.10.27",
    "@types/getenv": "^0.7.0",
    "@types/jest": "^24.0.15",
    "@types/raven": "^2.5.3",
    "aws-lambda": "^1.0.5",
    "jest": "^24.8.0",
    "serverless-sentry-lib": "^1.1.2",
    "ts-jest": "^24.0.2"
  },
  "peerDependencies": {
    "@home-ht/common": "^1.2.0",
    "aws-lambda": "^1.0.5",
    "raven": "^2.6.4",
    "serverless-sentry-lib": "^1.1.2"
  }
}
